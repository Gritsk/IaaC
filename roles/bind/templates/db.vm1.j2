$TTL	604800
vm1.	IN	SOA	vm2.{{ domain }}{{ top_level_domain }} master.{{ domain }}{{ top_level_domain }} (
			      2		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL

vm1.	IN	NS	vm2.{{ domain }}{{ top_level_domain }}
vm1.	IN	A	{{ hostvars['vm1']['ansible_default_ipv4']['address'] }}
