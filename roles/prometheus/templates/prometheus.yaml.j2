scrape_configs:

  - job_name: linux
    static_configs:
      - targets:
          {% for vm in groups['all'] %}
            - {{ hostvars[vm]['inventory_hostname'] }}.{{ domain }}:9100 
          {% endfor -%}
